<?xml version="1.0" encoding="utf-8"?>
<!--
  This manifest fragment is merged into the main app manifest by Gradle.
  The Expo config plugin (plugins/withAndroidIME.js) handles this automatically
  during `expo prebuild`.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <service
            android:name="com.moji.keyboard.MojiIMEService"
            android:label="Moji Emoji Keyboard"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>

</manifest>
